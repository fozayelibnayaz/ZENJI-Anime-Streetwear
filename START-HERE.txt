ZENJI — FINAL CLEAN COMPLETE PROJECT — START HERE
===================================================

This folder is the ENTIRE project, first to last, matching the live dev
sandbox (:3000) exactly — build id U6cfz0OiMze6phGx_Thkk.

  /         full Next.js source: 14 routes, all components, content, libs,
            88 tests, configs, clean docs (README, USER-MANUAL, DEVELOPER-GUIDE,
            DEPLOYMENT-NOTES)
  /docs     the BUILT static site (GitHub Pages ready, .nojekyll included)

PUSH TO YOUR REPO (exact commands — run in a terminal)
  git clone https://github.com/fozayelibnayaz/ZENJI-Anime-Streetwear.git zenji-deploy
  cd zenji-deploy
  find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
  cp -R ~/Downloads/ZENJI-final/. .
  ls docs/index.html docs/.nojekyll src/app/account/page.tsx   # all must print
  git add -A
  git commit -m "ZENJI complete project"
  git push origin main

  GitHub → Settings → Pages → Branch main, Folder /docs → Save.
  Wait ~1 min. Hard refresh (Cmd+Shift+R).

IT LANDED WHEN
  - nav shows COUNTER + Sign in; /account /counter /arcade load
  - view-source shows _next/static/U6cfz0OiMze6phGx_Thkk/

RUN LOCALLY
  npm ci && npm run dev   (http://localhost:3000)
  npm test                (88 tests)

MANIFEST.txt lists every file in this folder.
